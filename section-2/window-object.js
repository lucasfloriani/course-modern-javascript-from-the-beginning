// WINDOW METHODS / OBJECTS / PROPERTIES
//window.console.log(123);

// Alert
//window.alert('Hello World');

// Promt
// const input = prompt();
// alert(input);

// Confirm
// if(confirm("Are you sure")){
//   console.log("YES");
// } else {
//   console.log("NO");
// }

let val;

// Outter height and width with including toolbar/scroll
// val = window.outerHeight;
// val = window.outerWidth;


// Inner height and width not including toolbar/scroll
// val = window.innerHeight;
// val = window.innerWidth;

// Scroll Points
// val = window.scrollY;
// val = window.scrollX;

// Location Object
// val = window.location;
// val = window.location.hostname;
// val = window.location.port;
// val = window.location.href;
// val = window.location.search; // Variables in GET

// Redirect
// val = window.location.href = "http://google.com";
// Reload
// window.location.reload();

// History object
// val = window.history;
// window.history.go(-1); // Return pages
// val = window.history.length; // Pages before

// Navigator Object
// val = window.navigator; // Info about navigator (Chrome, Firefox, IE, etc)
// val = window.navigator.appName; // Aways Nepscape
// val = window.navigator.appVersion; // Versão do navegador
// val = window.navigator.userAgent; // Versão do navegador
// val = window.navigator.platform; // Versão do Sistema Operacional
// val = window.navigator.vendor;
// val = window.navigator.language; // Linguagem do navegador

console.log(val);